apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespaces are set explicitly in the manifests (no namespace transformer here,
# so ServiceAccount subjects in the ClusterRoleBindings are not rewritten).
resources:
  - epp-metrics-rbac.yaml
  - epp-servicemonitor.yaml
